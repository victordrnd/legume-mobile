import { DefaultTheme } from "react-native-paper";

 const theme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: '#40a500',
    accent: '#f19200',
    background: '#efefef',
  },
};

export default theme